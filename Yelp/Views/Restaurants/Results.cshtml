@{
  Layout = "_Layout";
}

@using Yelp.Models;

@if (@Model.Count == 0)
{
  <p> Please add a restaurant!</p>
}

@foreach (Restaurant restaurant in Model)
{
  <div>
    <h4>@Html.ActionLink($"{restaurant.Name}","Details", new {id = restaurant.RestaurantId})</h4>
  </div>
}

<div>@Html.ActionLink("Return Home", "Index", "Home")</div>